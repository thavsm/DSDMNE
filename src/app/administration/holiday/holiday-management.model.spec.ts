import { HolidayManagement } from './holiday-management.model';

describe('HolidayManagement', () => {
  it('should create an instance', () => {
    expect(new HolidayManagement()).toBeTruthy();
  });
});
